export const ListingAssets = ({ listItems, id }) => {
    return (
        <>
            <div>
                <h3>
                    {listItems.name}
                </h3>
            </div>
        </>
    )
}